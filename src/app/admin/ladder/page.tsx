import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export default function AdminLadderPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Manage Ladder</CardTitle>
        <CardDescription>
          This section is for managing the competitive ladder. Functionality to be added.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">Ladder management UI will be displayed here.</p>
      </CardContent>
    </Card>
  );
}
