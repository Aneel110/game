import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export default function AdminPrizesPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Manage Prizes</CardTitle>
        <CardDescription>
          This section is for managing prize distribution and redemption. Functionality to be added.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">Prize management UI will be displayed here.</p>
      </CardContent>
    </Card>
  );
}
